# TeamsX Badge Test - 冒烟测试用例

## 概述
本文档包含针对 TeamsX 多智能体团队协作系统的冒烟测试用例，验证核心功能是否正常工作。

---

## 测试用例 1: 团队创建与配置
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-001 |
| **测试目标** | 验证 TeamsX 能够成功创建团队并配置成员 |
| **前置条件** | DeepSeek Harness 正常运行 |
| **测试步骤** | 1. 调用 `teamsx_create` 创建团队<br>2. 设置团队名称、描述、审批模式<br>3. 调用 `teamsx_add_member` 添加成员<br>4. 调用 `teamsx_status` 验证团队状态 |
| **预期结果** | 团队成功创建，成员正确添加，状态显示为 running |
| **通过标准** | 无错误返回，团队状态正确 |

---

## 测试用例 2: 任务创建与依赖管理
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-002 |
| **测试目标** | 验证任务创建、依赖设置和调度功能 |
| **前置条件** | 团队已创建，成员已添加 |
| **测试步骤** | 1. 调用 `teamsx_create_task` 创建任务<br>2. 设置任务依赖关系<br>3. 验证任务依赖解除后状态变为 ready<br>4. 调用 `teamsx_status` 查看任务状态 |
| **预期结果** | 任务正确创建，依赖关系正确管理，状态流转正常 |
| **通过标准** | 任务状态正确变化，依赖解除后自动解锁 |

---

## 测试用例 3: 任务认领与执行
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-003 |
| **测试目标** | 验证成员能够认领任务并更新执行状态 |
| **前置条件** | 任务已创建且状态为 ready |
| **测试步骤** | 1. 成员调用 `teamsx_claim_task` 认领任务<br>2. 记录返回的 attempt_id<br>3. 调用 `teamsx_update_task` 更新状态为 in_progress<br>4. 完成工作后更新状态为 completed |
| **预期结果** | 任务成功认领，attempt_id 有效，状态正确更新 |
| **通过标准** | 认领返回有效 attempt_id，状态变更成功 |

---

## 测试用例 4: 消息传递与通信
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-004 |
| **测试目标** | 验证成员间消息传递功能 |
| **前置条件** | 团队运行中，成员已添加 |
| **测试步骤** | 1. 调用 `teamsx_send_message` 向 captain 发送消息<br>2. 调用 `teamsx_send_message` 向其他成员发送消息<br>3. 验证消息送达 |
| **预期结果** | 消息成功发送到目标成员的邮箱 |
| **通过标准** | 发送无错误，消息内容完整 |

---

## 测试用例 5: 任务状态查询
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-005 |
| **测试目标** | 验证团队状态和任务状态查询功能 |
| **前置条件** | 团队运行中，有多个任务 |
| **测试步骤** | 1. 调用 `teamsx_status` 获取团队快照<br>2. 验证成员列表和活动状态<br>3. 验证任务列表和状态<br>4. 验证依赖关系显示 |
| **预期结果** | 返回完整的团队状态，包含成员和任务信息 |
| **通过标准** | 状态信息准确完整 |

---

## 测试用例 6: 任务重分配
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-006 |
| **测试目标** | 验证任务重分配功能 |
| **前置条件** | 任务已分配给成员 A |
| **测试步骤** | 1. 调用 `teamsx_reassign_task` 将任务重分配给成员 B<br>2. 验证原成员 A 的 attempt_id 失效<br>3. 验证新成员 B 可以认领任务 |
| **预期结果** | 任务成功重分配，旧 attempt_id 失效 |
| **通过标准** | 重分配成功，状态正确更新 |

---

## 测试用例 7: 质量门禁验证
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-007 |
| **测试目标** | 验证质量门禁任务的评审流程 |
| **前置条件** | 有质量门禁类型的完成任务 |
| **测试步骤** | 1. 成员完成质量门禁任务<br>2. 评审人调用 `teamsx_update_task` 设置 verdict=pass<br>3. 验证任务状态变为 completed<br>4. 测试 verdict=needs_revision 的场景 |
| **预期结果** | 评审通过时任务完成，需要修改时正确标记 |
| **通过标准** | verdict 状态正确，findings 正确记录 |

---

## 测试用例 8: 并发任务处理
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-008 |
| **测试目标** | 验证多个成员同时处理独立任务的能力 |
| **前置条件** | 多个无依赖的 ready 任务 |
| **测试步骤** | 1. 成员 A 认领任务 1<br>2. 成员 B 同时认领任务 2<br>3. 两者并行执行<br>4. 验证两个任务都成功完成 |
| **预期结果** | 并发认领和执行无冲突 |
| **通过标准** | 两个任务都成功完成，无竞态条件 |

---

## 测试用例 9: 任务取消
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-009 |
| **测试目标** | 验证任务取消功能 |
| **前置条件** | 有进行中的任务 |
| **测试步骤** | 1. Captain 调用 `teamsx_update_task` 设置 status=cancelled<br>2. 验证任务状态变为 cancelled<br>3. 验证下游依赖任务被解锁 |
| **预期结果** | 任务成功取消，依赖任务正确解锁 |
| **通过标准** | 取消状态正确，依赖管理正确 |

---

## 测试用例 10: 团队删除与清理
| 项目 | 内容 |
|------|------|
| **用例ID** | TC-010 |
| **测试目标** | 验证团队删除和资源清理功能 |
| **前置条件** | 团队已完成所有任务 |
| **测试步骤** | 1. 确认所有任务为终态<br>2. 调用 `teamsx_delete` 删除团队<br>3. 验证团队数据已清理 |
| **预期结果** | 团队成功删除，资源正确释放 |
| **通过标准** | 删除无错误，状态查询返回空 |

---

## 执行记录

| 用例ID | 执行日期 | 执行人 | 结果 | 备注 |
|--------|----------|--------|------|------|
| TC-001 | - | - | - | 待执行 |
| TC-002 | - | - | - | 待执行 |
| TC-003 | - | - | - | 待执行 |
| TC-004 | - | - | - | 待执行 |
| TC-005 | - | - | - | 待执行 |
| TC-006 | - | - | - | 待执行 |
| TC-007 | - | - | - | 待执行 |
| TC-008 | - | - | - | 待执行 |
| TC-009 | - | - | - | 待执行 |
| TC-010 | - | - | - | 待执行 |

---

*文档创建时间: $(date)*
*文档版本: 1.0*
*作者: tester (badge-test team)*
